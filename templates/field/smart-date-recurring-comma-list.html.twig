{% for key, value in values -%}
  {{ not loop.first and not loop.last ? ', '|t({}, {context: 'Rule text'}) -}}
  {{ loop.last ? ', and '|t({}, {context: 'Rule text'}) -}}
  {% if day_counts[value] > 1 %}
    {{ (value ~ 's') -}}
  {% else %}
    {{ value -}}
  {% endif %}
{% endfor -%}
